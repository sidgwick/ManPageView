" Additional ftplugin by Ted Tibbetts <http://github.com/intuited>

" Manpages are generated to the width of the window,
" so don't scroll over to empty space.
" This really shouldn't be necessary,
" since Vim shouldn't do this in case like this
" where there is no text beyond the edge of the screen.
setlocal sidescrolloff=0
